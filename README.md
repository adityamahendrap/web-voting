# Web Musma Teknik 2023

``` bash
# install dependencies
$ composer update
$ composer install

# env
$ cp .env.example .env

# generate key
$ php artisan key:generate

# run migration
$ php artisan migrate

# run seeder
$ php artisan db:seed

# run dev server
$ php artisan serve
```
